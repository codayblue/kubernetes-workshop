apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: awesome-app
spec:
  rules:
  - host: 127-0-0-1.sslip.io
    http:
      paths:
      - backend:
          service:
            name: awesome-app
            port:
              number: 8080
        path: /
        pathType: Prefix
